<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaveAnimal">
        <!--        <activity-->
<!--            android:name=".save_memberEdit"-->
<!--            android:exported="false" />-->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SaveAnimal.NoActionBar">

        <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SaveMainActivity" />
        <activity android:name=".GenMainActivity" />
        <activity android:name=".SaveMemberInformEditActivity" />
        <activity android:name=".GenNoticeBoard" />
        <activity android:name=".SaveAnnouncementWrite"/>
        <activity android:name=".GenMemberInformEdit"/>
        <activity android:name=".ResultMainActivity"/>
        <activity android:name=".ResultActivity"/>
        <activity android:name=".MyPickActivity"/>
        <activity android:name=".MyApplicationActivity"/>
        <activity android:name=".PostDetailCustomActivity"/>
        <activity android:name=".SheleterProfileActivity"
            android:theme="@style/Theme.SaveAnimal.NoActionBar"/>


    </application>

</manifest>